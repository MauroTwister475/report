import { Root } from "./Root"
import { Input } from "./Input"
import { Select } from "./Select1"
import { Button } from "./Button"

export const Form = {
  Root, 
  Input, 
  Button, 
  Select,
}